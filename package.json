{
  "name": "dsa",
  "version": "1.0.0",
  "private": true,
  "description": "Data structures and algorithms practice in TypeScript",
  "type": "module",
  "scripts": {
    "test": "node --test 'src/**/*.test.ts' 'tests/**/*.test.ts'",
    "test:watch": "node --test --watch 'src/**/*.test.ts' 'tests/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
